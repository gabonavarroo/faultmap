# faultmap
Python library that automatically discovers statistically significant failure patterns in LLM evaluations using embedding-space clustering with multiple hypothesis correction. Uses entropy and Monte Carlo Simulation for Scoring output.
